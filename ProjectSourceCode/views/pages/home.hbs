{{!-- Cards that pull data from the database  --}}
{{> message}}
<div id="homebody">
    {{#each pet}}
    <div class="row">
        <div class="column">
            <div class="card">
                <div class="container">
                    <div class="pet-info">
                        <img src="../../src/resources/img/{{this.image_url}}" alt="{{this.image_url}}" />
                        <h3>Name: {{this.name}}</h3>
                        <p>Class: {{this.class}}</p>
                        <p>Age: {{this.age}}</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="column">
            <div class="card">
                <div class="container">
                    <div class="pet-info">
                        <img src="/img/{{this.image_url}}" alt="{{this.name}}" />
                        <h3>Name: {{this.name}}</h3>
                        <p>Class: {{this.class}}</p>
                        <p>Age: {{this.age}}</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="column">
            <div class="card">
                <div class="container">
                    <div class="pet-info">
                        <img src="/img/{{this.image_url}}" alt="{{this.name}}" />
                        <h3>Name: {{this.name}}</h3>
                        <p>Class: {{this.class}}</p>
                        <p>Age: {{this.age}}</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="column">
            <div class="card">
                <div class="container">
                    <div class="pet-info">
                        <img src="/img/{{this.image_url}}" alt="{{this.name}}" />
                        <h3>Name: {{this.name}}</h3>
                        <p>Class: {{this.class}}</p>
                        <p>Age: {{this.age}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{/each}}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>